'use client';

const AddToCart = () => {
  return (
    <div>
      <button
        className="btn btn-active btn-primary"
        onClick={() => console.log('clicked')}
      >
        Add to Cart
      </button>
    </div>
  );
};
export default AddToCart;
